# Static value per project to ease the creation of front/back strict port for dev, not in conflict with other projects
PROJECT_ID=3

# Workspace Configuration (for parallel development) is done in .env.local
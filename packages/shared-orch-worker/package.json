{
	"name": "shared-orch-worker",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "tsc --build tsconfig.json",
		"dev": "tsc --build tsconfig.json --watch",
		"test": "vitest run --passWithNoTests",
		"test:watch": "vitest",
		"lint": "eslint src",
		"lint:fix": "eslint src --fix",
		"clean": "rimraf dist dist-types"
	},
	"dependencies": {
		"zod": "^4.1.13"
	},
	"devDependencies": {
		"tsx": "^4.7.0",
		"typescript": "^7.0.0",
		"vitest": "^4.0.14"
	}
}

{
	"name": "extension-points",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"exports": {
		".": {
			"types": "./dist-types/index.d.ts",
			"default": "./src/index.ts"
		},
		"./extension-points.json": "./extension-points.json"
	},
	"scripts": {
		"build": "tsc --build tsconfig.json",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:coverage": "vitest run --coverage",
		"lint": "eslint src",
		"lint:fix": "eslint src --fix",
		"clean": "rimraf dist dist-types"
	},
	"devDependencies": {
		"@types/node": "^24.0.0",
		"typescript": "^7.0.0",
		"vitest": "^4.0.14"
	}
}

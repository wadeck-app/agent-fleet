{
	"name": "cli",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"bin": {
		"fleet-task": "./dist/entry-point.js"
	},
	"scripts": {
		"build": "tsc --build tsconfig.json",
		"dev": "tsx src/entry-point.ts",
		"test": "vitest run",
		"lint": "echo 'No ESLint configured for cli'",
		"clean": "rimraf dist"
	},
	"dependencies": {
		"orchestrator": "*",
		"shared-common": "*",
		"shared-orch-worker": "*"
	},
	"devDependencies": {
		"test-utils": "*",
		"typescript": "^5.3.3",
		"vitest": "^4.0.14",
		"tsx": "^4.7.0"
	}
}

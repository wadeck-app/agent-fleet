// AUTO-GENERATED FILE - DO NOT EDIT MANUALLY
// Generated by: scripts/generate-visual-regression-tests.js
// Total stories: 334
//
// To regenerate: npm run test:visual:generate
// Documentation: See scripts/generate-visual-regression-tests.js for configuration
import { expect, test } from '../playwright-hooks/hooks-storybook';
import { ensureStoryExists } from '../utils/visualTestHelpers';

test.describe('visual', () => {
	test('Components/LoadingDots - AllSizes', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-loadingdots--all-sizes');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-loadingdots--all-sizes');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'LoadingDots', 'AllSizes.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/LoadingDots - Large', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-loadingdots--large');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-loadingdots--large');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'LoadingDots', 'Large.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/LoadingDots - Medium', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-loadingdots--medium');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-loadingdots--medium');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'LoadingDots', 'Medium.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/LoadingDots - OnDarkBackground', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-loadingdots--on-dark-background');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-loadingdots--on-dark-background');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'LoadingDots', 'OnDarkBackground.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/LoadingDots - Small', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-loadingdots--small');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-loadingdots--small');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'LoadingDots', 'Small.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/LoadingDots - WithCustomColor', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-loadingdots--with-custom-color');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-loadingdots--with-custom-color');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'LoadingDots', 'WithCustomColor.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/Table - Basic', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-table--basic');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-table--basic');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Table', 'Table', 'Basic.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/Table - Empty', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-table--empty');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-table--empty');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Table', 'Table', 'Empty.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/Table - LargeDataset', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-table--large-dataset');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-table--large-dataset');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Table', 'Table', 'LargeDataset.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/Table - Loading', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-table--loading');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-table--loading');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Table', 'Table', 'Loading.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/Table - WithActions', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-table--with-actions');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-table--with-actions');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Table', 'Table', 'WithActions.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/Table - WithCustomRowClasses', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-table--with-custom-row-classes');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-table--with-custom-row-classes');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Table', 'Table', 'WithCustomRowClasses.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/Table - WithSelection', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-table--with-selection');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-table--with-selection');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Table', 'Table', 'WithSelection.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/Toast - AllTypes', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-toast--all-types');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-toast--all-types');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Toast', 'AllTypes.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/Toast - Error', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-toast--error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-toast--error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Toast', 'Error.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/Toast - Info', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-toast--info');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-toast--info');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Toast', 'Info.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/Toast - LongMessage', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-toast--long-message');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-toast--long-message');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Toast', 'LongMessage.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/Toast - Success', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-toast--success');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-toast--success');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Toast', 'Success.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Components/Toast - Warning', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'components-toast--warning');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=components-toast--warning');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Toast', 'Warning.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookForm - Comparison', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-bookform--comparison');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-bookform--comparison');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookForm', 'Comparison.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookForm - CustomLabel', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-bookform--custom-label');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-bookform--custom-label');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookForm', 'CustomLabel.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookForm - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-bookform--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-bookform--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookForm', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookForm - EditMode', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-bookform--edit-mode');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-bookform--edit-mode');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookForm', 'EditMode.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookForm - EditModeISBNConflict', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-bookform--edit-mode-i-s-b-n-conflict');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-bookform--edit-mode-i-s-b-n-conflict');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookForm', 'EditModeISBNConflict.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookForm - EditModeWithSaveISBN', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-bookform--edit-mode-with-save-i-s-b-n');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-bookform--edit-mode-with-save-i-s-b-n');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookForm', 'EditModeWithSaveISBN.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookForm - InContext', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-bookform--in-context');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-bookform--in-context');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookForm', 'InContext.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookForm - ISBNTaken', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-bookform--i-s-b-n-taken');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-bookform--i-s-b-n-taken');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookForm', 'ISBNTaken.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookForm - PartialData', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-bookform--partial-data');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-bookform--partial-data');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookForm', 'PartialData.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookForm - ValidationDemo', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-bookform--validation-demo');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-bookform--validation-demo');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookForm', 'ValidationDemo.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookForm - WithISBNCheck', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-bookform--with-i-s-b-n-check');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-bookform--with-i-s-b-n-check');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookForm', 'WithISBNCheck.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookTable - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-booktable--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-booktable--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookTable', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookTable - Empty', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-booktable--empty');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-booktable--empty');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookTable', 'Empty.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookTable - IncompleteData', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-booktable--incomplete-data');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-booktable--incomplete-data');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookTable', 'IncompleteData.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookTable - InContext', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-booktable--in-context');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-booktable--in-context');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookTable', 'InContext.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookTable - LargeDataset', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-booktable--large-dataset');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-booktable--large-dataset');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookTable', 'LargeDataset.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookTable - SingleBook', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-booktable--single-book');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-booktable--single-book');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookTable', 'SingleBook.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/BookTable - WithEdit', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-booktable--with-edit');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-booktable--with-edit');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'BookTable', 'WithEdit.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/CheckboxField - Checked', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-checkboxfield--checked');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-checkboxfield--checked');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'CheckboxField', 'Checked.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/CheckboxField - CheckedDisabled', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-checkboxfield--checked-disabled');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-checkboxfield--checked-disabled');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'CheckboxField', 'CheckedDisabled.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/CheckboxField - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-checkboxfield--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-checkboxfield--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'CheckboxField', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/CheckboxField - Disabled', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-checkboxfield--disabled');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-checkboxfield--disabled');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'CheckboxField', 'Disabled.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/CheckboxField - Required', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-checkboxfield--required');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-checkboxfield--required');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'CheckboxField', 'Required.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/CheckboxField - WithError', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-checkboxfield--with-error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-checkboxfield--with-error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'CheckboxField', 'WithError.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/DateField - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-datefield--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-datefield--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'DateField', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/DateField - Disabled', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-datefield--disabled');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-datefield--disabled');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'DateField', 'Disabled.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/DateField - Required', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-datefield--required');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-datefield--required');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'DateField', 'Required.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/DateField - WithError', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-datefield--with-error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-datefield--with-error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'DateField', 'WithError.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/DateField - WithMinMax', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-datefield--with-min-max');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-datefield--with-min-max');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'DateField', 'WithMinMax.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/DateField - WithValue', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-datefield--with-value');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-datefield--with-value');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'DateField', 'WithValue.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/IntegerField - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-integerfield--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-integerfield--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'IntegerField', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/IntegerField - Required', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-integerfield--required');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-integerfield--required');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'IntegerField', 'Required.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/IntegerField - WithError', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-integerfield--with-error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-integerfield--with-error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'IntegerField', 'WithError.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/IntegerField - WithMinMax', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-integerfield--with-min-max');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-integerfield--with-min-max');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'IntegerField', 'WithMinMax.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/IntegerField - WithValue', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-integerfield--with-value');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-integerfield--with-value');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'IntegerField', 'WithValue.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/NumberField - Decimal', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-numberfield--decimal');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-numberfield--decimal');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'NumberField', 'Decimal.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/NumberField - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-numberfield--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-numberfield--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'NumberField', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/NumberField - Required', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-numberfield--required');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-numberfield--required');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'NumberField', 'Required.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/NumberField - WithError', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-numberfield--with-error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-numberfield--with-error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'NumberField', 'WithError.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/NumberField - WithMinMax', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-numberfield--with-min-max');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-numberfield--with-min-max');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'NumberField', 'WithMinMax.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/RadioGroupField - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-radiogroupfield--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-radiogroupfield--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'RadioGroupField', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/RadioGroupField - Horizontal', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-radiogroupfield--horizontal');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-radiogroupfield--horizontal');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'RadioGroupField', 'Horizontal.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/RadioGroupField - Interactive', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-radiogroupfield--interactive');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-radiogroupfield--interactive');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'RadioGroupField', 'Interactive.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/RadioGroupField - LongLabels', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-radiogroupfield--long-labels');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-radiogroupfield--long-labels');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'RadioGroupField', 'LongLabels.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/RadioGroupField - MultipleInForm', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-radiogroupfield--multiple-in-form');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-radiogroupfield--multiple-in-form');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'RadioGroupField', 'MultipleInForm.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/RadioGroupField - NotificationPreferences', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-radiogroupfield--notification-preferences');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-radiogroupfield--notification-preferences');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(
			['features', 'form', 'fields', 'RadioGroupField', 'NotificationPreferences.png'],
			{ maxDiffPixels: 100 }
		);
	});

	test('Features/Form/Fields/RadioGroupField - Required', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-radiogroupfield--required');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-radiogroupfield--required');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'RadioGroupField', 'Required.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/RadioGroupField - ShippingMethod', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-radiogroupfield--shipping-method');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-radiogroupfield--shipping-method');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'RadioGroupField', 'ShippingMethod.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/RadioGroupField - ThemeSelection', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-radiogroupfield--theme-selection');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-radiogroupfield--theme-selection');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'RadioGroupField', 'ThemeSelection.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/RadioGroupField - ValidationStates', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-radiogroupfield--validation-states');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-radiogroupfield--validation-states');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'RadioGroupField', 'ValidationStates.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/RadioGroupField - Vertical', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-radiogroupfield--vertical');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-radiogroupfield--vertical');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'RadioGroupField', 'Vertical.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/RadioGroupField - WithDisabledOption', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-radiogroupfield--with-disabled-option');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-radiogroupfield--with-disabled-option');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(
			['features', 'form', 'fields', 'RadioGroupField', 'WithDisabledOption.png'],
			{ maxDiffPixels: 100 }
		);
	});

	test('Features/Form/Fields/RadioGroupField - WithError', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-radiogroupfield--with-error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-radiogroupfield--with-error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'RadioGroupField', 'WithError.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SelectField - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-selectfield--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-selectfield--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SelectField', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SelectField - Disabled', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-selectfield--disabled');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-selectfield--disabled');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SelectField', 'Disabled.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SelectField - Required', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-selectfield--required');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-selectfield--required');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SelectField', 'Required.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SelectField - WithError', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-selectfield--with-error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-selectfield--with-error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SelectField', 'WithError.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SelectField - WithValue', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-selectfield--with-value');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-selectfield--with-value');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SelectField', 'WithValue.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SwitchField - AccountSecurity', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-switchfield--account-security');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-switchfield--account-security');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SwitchField', 'AccountSecurity.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SwitchField - Checked', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-switchfield--checked');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-switchfield--checked');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SwitchField', 'Checked.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SwitchField - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-switchfield--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-switchfield--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SwitchField', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SwitchField - FeatureToggles', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-switchfield--feature-toggles');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-switchfield--feature-toggles');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SwitchField', 'FeatureToggles.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SwitchField - FormWithValidation', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-switchfield--form-with-validation');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-switchfield--form-with-validation');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SwitchField', 'FormWithValidation.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SwitchField - Interactive', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-switchfield--interactive');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-switchfield--interactive');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SwitchField', 'Interactive.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SwitchField - PrivacySettings', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-switchfield--privacy-settings');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-switchfield--privacy-settings');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SwitchField', 'PrivacySettings.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SwitchField - Required', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-switchfield--required');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-switchfield--required');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SwitchField', 'Required.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SwitchField - SettingsForm', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-switchfield--settings-form');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-switchfield--settings-form');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SwitchField', 'SettingsForm.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SwitchField - ValidationStates', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-switchfield--validation-states');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-switchfield--validation-states');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SwitchField', 'ValidationStates.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SwitchField - WithDescription', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-switchfield--with-description');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-switchfield--with-description');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SwitchField', 'WithDescription.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/SwitchField - WithDescriptionAndError', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-switchfield--with-description-and-error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-switchfield--with-description-and-error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(
			['features', 'form', 'fields', 'SwitchField', 'WithDescriptionAndError.png'],
			{ maxDiffPixels: 100 }
		);
	});

	test('Features/Form/Fields/SwitchField - WithError', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-switchfield--with-error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-switchfield--with-error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'SwitchField', 'WithError.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/TextAreaField - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-textareafield--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-textareafield--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'TextAreaField', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/TextAreaField - Required', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-textareafield--required');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-textareafield--required');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'TextAreaField', 'Required.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/TextAreaField - TallTextArea', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-textareafield--tall-text-area');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-textareafield--tall-text-area');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'TextAreaField', 'TallTextArea.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/TextAreaField - WithError', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-textareafield--with-error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-textareafield--with-error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'TextAreaField', 'WithError.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/TextAreaField - WithValue', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-textareafield--with-value');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-textareafield--with-value');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'TextAreaField', 'WithValue.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/TextField - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-textfield--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-textfield--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'TextField', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/TextField - Disabled', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-textfield--disabled');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-textfield--disabled');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'TextField', 'Disabled.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/TextField - Password', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-textfield--password');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-textfield--password');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'TextField', 'Password.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/TextField - Required', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-textfield--required');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-textfield--required');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'TextField', 'Required.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Fields/TextField - WithError', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-fields-textfield--with-error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-fields-textfield--with-error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'fields', 'TextField', 'WithError.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/CheckboxInput - Checked', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-checkboxinput--checked');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-checkboxinput--checked');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'CheckboxInput', 'Checked.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/CheckboxInput - CheckedDisabled', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-checkboxinput--checked-disabled');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-checkboxinput--checked-disabled');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'CheckboxInput', 'CheckedDisabled.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/CheckboxInput - Disabled', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-checkboxinput--disabled');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-checkboxinput--disabled');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'CheckboxInput', 'Disabled.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/CheckboxInput - Unchecked', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-checkboxinput--unchecked');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-checkboxinput--unchecked');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'CheckboxInput', 'Unchecked.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/DateInput - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-dateinput--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-dateinput--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'DateInput', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/DateInput - Disabled', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-dateinput--disabled');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-dateinput--disabled');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'DateInput', 'Disabled.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/DateInput - WithMinMax', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-dateinput--with-min-max');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-dateinput--with-min-max');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'DateInput', 'WithMinMax.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/DateInput - WithValue', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-dateinput--with-value');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-dateinput--with-value');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'DateInput', 'WithValue.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/NumberInput - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-numberinput--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-numberinput--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'NumberInput', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/NumberInput - Disabled', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-numberinput--disabled');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-numberinput--disabled');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'NumberInput', 'Disabled.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/NumberInput - Integer', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-numberinput--integer');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-numberinput--integer');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'NumberInput', 'Integer.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/NumberInput - WithMinMax', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-numberinput--with-min-max');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-numberinput--with-min-max');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'NumberInput', 'WithMinMax.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/NumberInput - WithStep', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-numberinput--with-step');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-numberinput--with-step');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'NumberInput', 'WithStep.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/SelectInput - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-selectinput--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-selectinput--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'SelectInput', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/SelectInput - Disabled', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-selectinput--disabled');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-selectinput--disabled');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'SelectInput', 'Disabled.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/SelectInput - WithPlaceholder', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-selectinput--with-placeholder');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-selectinput--with-placeholder');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'SelectInput', 'WithPlaceholder.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/SelectInput - WithValue', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-selectinput--with-value');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-selectinput--with-value');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'SelectInput', 'WithValue.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/TextAreaInput - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-textareainput--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-textareainput--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'TextAreaInput', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/TextAreaInput - Disabled', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-textareainput--disabled');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-textareainput--disabled');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'TextAreaInput', 'Disabled.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/TextAreaInput - TallTextArea', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-textareainput--tall-text-area');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-textareainput--tall-text-area');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'TextAreaInput', 'TallTextArea.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/TextAreaInput - WithValue', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-textareainput--with-value');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-textareainput--with-value');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'TextAreaInput', 'WithValue.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/TextInput - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-textinput--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-textinput--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'TextInput', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/TextInput - Disabled', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-textinput--disabled');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-textinput--disabled');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'TextInput', 'Disabled.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/TextInput - Email', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-textinput--email');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-textinput--email');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'TextInput', 'Email.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/TextInput - Password', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-textinput--password');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-textinput--password');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'TextInput', 'Password.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/Form/Inputs/TextInput - WithValue', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-form-inputs-textinput--with-value');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-form-inputs-textinput--with-value');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['features', 'form', 'inputs', 'TextInput', 'WithValue.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientForm - Comparison', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredientform--comparison');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredientform--comparison');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientForm', 'Comparison.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientForm - CustomLabel', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredientform--custom-label');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredientform--custom-label');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientForm', 'CustomLabel.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientForm - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredientform--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredientform--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientForm', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientForm - EditMode', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredientform--edit-mode');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredientform--edit-mode');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientForm', 'EditMode.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientForm - HighProteinFood', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredientform--high-protein-food');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredientform--high-protein-food');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientForm', 'HighProteinFood.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientForm - InContext', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredientform--in-context');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredientform--in-context');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientForm', 'InContext.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientForm - LiquidIngredient', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredientform--liquid-ingredient');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredientform--liquid-ingredient');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientForm', 'LiquidIngredient.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientForm - ValidationDemo', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredientform--validation-demo');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredientform--validation-demo');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientForm', 'ValidationDemo.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientForm - Vegetable', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredientform--vegetable');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredientform--vegetable');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientForm', 'Vegetable.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientTable - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredienttable--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredienttable--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientTable', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientTable - Empty', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredienttable--empty');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredienttable--empty');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientTable', 'Empty.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientTable - HighProtein', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredienttable--high-protein');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredienttable--high-protein');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientTable', 'HighProtein.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientTable - InContext', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredienttable--in-context');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredienttable--in-context');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientTable', 'InContext.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientTable - LargeDataset', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredienttable--large-dataset');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredienttable--large-dataset');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientTable', 'LargeDataset.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientTable - NoCategory', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredienttable--no-category');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredienttable--no-category');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientTable', 'NoCategory.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientTable - SingleIngredient', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredienttable--single-ingredient');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredienttable--single-ingredient');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientTable', 'SingleIngredient.png'], {
			maxDiffPixels: 100,
		});
	});

	test('Features/IngredientTable - WithEdit', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'features-ingredienttable--with-edit');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=features-ingredienttable--with-edit');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'features', 'IngredientTable', 'WithEdit.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Button - AllSizes', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-button--all-sizes');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-button--all-sizes');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Button', 'AllSizes.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Button - AllVariants', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-button--all-variants');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-button--all-variants');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Button', 'AllVariants.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Button - Destructive', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-button--destructive');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-button--destructive');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Button', 'Destructive.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Button - Disabled', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-button--disabled');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-button--disabled');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Button', 'Disabled.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Button - Ghost', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-button--ghost');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-button--ghost');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Button', 'Ghost.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Button - Interactive', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-button--interactive');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-button--interactive');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Button', 'Interactive.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Button - Large', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-button--large');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-button--large');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Button', 'Large.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Button - Medium', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-button--medium');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-button--medium');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Button', 'Medium.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Button - Outline', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-button--outline');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-button--outline');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Button', 'Outline.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Button - Primary', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-button--primary');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-button--primary');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Button', 'Primary.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Button - Secondary', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-button--secondary');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-button--secondary');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Button', 'Secondary.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Button - Small', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-button--small');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-button--small');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'Button', 'Small.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/EmptyState - Comparison', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-emptystate--comparison');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-emptystate--comparison');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'EmptyState', 'Comparison.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/EmptyState - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-emptystate--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-emptystate--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'EmptyState', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/EmptyState - InCard', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-emptystate--in-card');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-emptystate--in-card');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'EmptyState', 'InCard.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/EmptyState - NoBooks', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-emptystate--no-books');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-emptystate--no-books');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'EmptyState', 'NoBooks.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/EmptyState - NoSearchResults', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-emptystate--no-search-results');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-emptystate--no-search-results');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'EmptyState', 'NoSearchResults.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/EmptyState - TitleOnly', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-emptystate--title-only');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-emptystate--title-only');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'EmptyState', 'TitleOnly.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/EmptyState - WithAction', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-emptystate--with-action');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-emptystate--with-action');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'EmptyState', 'WithAction.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/EmptyState - WithIcon', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-emptystate--with-icon');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-emptystate--with-icon');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'EmptyState', 'WithIcon.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ErrorAlert - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-erroralert--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-erroralert--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ErrorAlert', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ErrorAlert - Dismissible', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-erroralert--dismissible');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-erroralert--dismissible');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ErrorAlert', 'Dismissible.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ErrorAlert - InFormContext', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-erroralert--in-form-context');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-erroralert--in-form-context');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ErrorAlert', 'InFormContext.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ErrorAlert - LongMessage', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-erroralert--long-message');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-erroralert--long-message');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ErrorAlert', 'LongMessage.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ErrorAlert - MultipleErrors', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-erroralert--multiple-errors');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-erroralert--multiple-errors');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ErrorAlert', 'MultipleErrors.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ErrorAlert - NetworkError', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-erroralert--network-error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-erroralert--network-error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ErrorAlert', 'NetworkError.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ErrorAlert - PermissionError', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-erroralert--permission-error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-erroralert--permission-error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ErrorAlert', 'PermissionError.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ErrorAlert - ShortMessage', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-erroralert--short-message');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-erroralert--short-message');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ErrorAlert', 'ShortMessage.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ErrorAlert - ValidationError', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-erroralert--validation-error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-erroralert--validation-error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ErrorAlert', 'ValidationError.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ErrorBoundary - CustomFallback', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-errorboundary--custom-fallback');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-errorboundary--custom-fallback');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ErrorBoundary', 'CustomFallback.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ErrorBoundary - Interactive', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-errorboundary--interactive');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-errorboundary--interactive');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ErrorBoundary', 'Interactive.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ErrorBoundary - MultipleBoundaries', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-errorboundary--multiple-boundaries');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-errorboundary--multiple-boundaries');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ErrorBoundary', 'MultipleBoundaries.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ErrorBoundary - NestedBoundaries', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-errorboundary--nested-boundaries');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-errorboundary--nested-boundaries');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ErrorBoundary', 'NestedBoundaries.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ErrorBoundary - NoError', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-errorboundary--no-error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-errorboundary--no-error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ErrorBoundary', 'NoError.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ErrorBoundary - WithError', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-errorboundary--with-error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-errorboundary--with-error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ErrorBoundary', 'WithError.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ErrorBoundary - WithErrorHandler', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-errorboundary--with-error-handler');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-errorboundary--with-error-handler');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ErrorBoundary', 'WithErrorHandler.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/LoadingSpinner - AllSizes', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-loadingspinner--all-sizes');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-loadingspinner--all-sizes');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'LoadingSpinner', 'AllSizes.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/LoadingSpinner - CustomMessage', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-loadingspinner--custom-message');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-loadingspinner--custom-message');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'LoadingSpinner', 'CustomMessage.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/LoadingSpinner - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-loadingspinner--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-loadingspinner--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'LoadingSpinner', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/LoadingSpinner - FullPage', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-loadingspinner--full-page');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-loadingspinner--full-page');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'LoadingSpinner', 'FullPage.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/LoadingSpinner - InCard', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-loadingspinner--in-card');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-loadingspinner--in-card');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'LoadingSpinner', 'InCard.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/LoadingSpinner - Large', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-loadingspinner--large');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-loadingspinner--large');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'LoadingSpinner', 'Large.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/LoadingSpinner - Medium', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-loadingspinner--medium');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-loadingspinner--medium');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'LoadingSpinner', 'Medium.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/LoadingSpinner - NoMessage', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-loadingspinner--no-message');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-loadingspinner--no-message');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'LoadingSpinner', 'NoMessage.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/LoadingSpinner - Small', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-loadingspinner--small');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-loadingspinner--small');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'LoadingSpinner', 'Small.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Accordion - ComplexContent', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-accordion--complex-content');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-accordion--complex-content');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Accordion', 'ComplexContent.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Accordion - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-accordion--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-accordion--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Accordion', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Accordion - Documentation', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-accordion--documentation');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-accordion--documentation');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Accordion', 'Documentation.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Accordion - FAQ', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-accordion--f-a-q');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-accordion--f-a-q');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Accordion', 'FAQ.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Accordion - KeyboardNavigation', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-accordion--keyboard-navigation');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-accordion--keyboard-navigation');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Accordion', 'KeyboardNavigation.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Accordion - LargeDataset', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-accordion--large-dataset');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-accordion--large-dataset');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Accordion', 'LargeDataset.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Accordion - Multiple', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-accordion--multiple');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-accordion--multiple');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Accordion', 'Multiple.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Accordion - MultipleWithDefaults', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-accordion--multiple-with-defaults');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-accordion--multiple-with-defaults');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Accordion', 'MultipleWithDefaults.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Accordion - NonCollapsible', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-accordion--non-collapsible');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-accordion--non-collapsible');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Accordion', 'NonCollapsible.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Accordion - SettingsPanel', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-accordion--settings-panel');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-accordion--settings-panel');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Accordion', 'SettingsPanel.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Accordion - WithDefaultValue', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-accordion--with-default-value');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-accordion--with-default-value');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Accordion', 'WithDefaultValue.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Accordion - WithDisabledItem', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-accordion--with-disabled-item');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-accordion--with-disabled-item');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Accordion', 'WithDisabledItem.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/AlertDialog - ComparisonWithNativeConfirm', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-alertdialog--comparison-with-native-confirm');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-alertdialog--comparison-with-native-confirm');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(
			['components', 'ui', 'radix', 'AlertDialog', 'ComparisonWithNativeConfirm.png'],
			{ maxDiffPixels: 100 }
		);
	});

	test('UI/Radix/AlertDialog - CustomLabels', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-alertdialog--custom-labels');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-alertdialog--custom-labels');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'AlertDialog', 'CustomLabels.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/AlertDialog - Danger', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-alertdialog--danger');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-alertdialog--danger');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'AlertDialog', 'Danger.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/AlertDialog - Info', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-alertdialog--info');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-alertdialog--info');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'AlertDialog', 'Info.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/AlertDialog - LongDescription', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-alertdialog--long-description');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-alertdialog--long-description');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'AlertDialog', 'LongDescription.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/AlertDialog - MultipleDialogs', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-alertdialog--multiple-dialogs');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-alertdialog--multiple-dialogs');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'AlertDialog', 'MultipleDialogs.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/AlertDialog - Warning', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-alertdialog--warning');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-alertdialog--warning');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'AlertDialog', 'Warning.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/AlertDialog - WithCancelCallback', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-alertdialog--with-cancel-callback');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-alertdialog--with-cancel-callback');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'AlertDialog', 'WithCancelCallback.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Checkbox - Checked', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-checkbox--checked');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-checkbox--checked');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Checkbox', 'Checked.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Checkbox - ComparisonWithNative', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-checkbox--comparison-with-native');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-checkbox--comparison-with-native');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Checkbox', 'ComparisonWithNative.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Checkbox - DisabledChecked', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-checkbox--disabled-checked');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-checkbox--disabled-checked');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Checkbox', 'DisabledChecked.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Checkbox - DisabledUnchecked', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-checkbox--disabled-unchecked');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-checkbox--disabled-unchecked');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Checkbox', 'DisabledUnchecked.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Checkbox - Indeterminate', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-checkbox--indeterminate');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-checkbox--indeterminate');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Checkbox', 'Indeterminate.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Checkbox - InlineLabel', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-checkbox--inline-label');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-checkbox--inline-label');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Checkbox', 'InlineLabel.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Checkbox - MultipleCheckboxes', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-checkbox--multiple-checkboxes');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-checkbox--multiple-checkboxes');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Checkbox', 'MultipleCheckboxes.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Checkbox - Required', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-checkbox--required');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-checkbox--required');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Checkbox', 'Required.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Checkbox - SelectAllPattern', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-checkbox--select-all-pattern');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-checkbox--select-all-pattern');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Checkbox', 'SelectAllPattern.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Checkbox - Unchecked', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-checkbox--unchecked');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-checkbox--unchecked');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Checkbox', 'Unchecked.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Checkbox - WithLabel', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-checkbox--with-label');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-checkbox--with-label');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Checkbox', 'WithLabel.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Dialog - Basic', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dialog--basic');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dialog--basic');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Dialog', 'Basic.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Dialog - FormDialog', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dialog--form-dialog');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dialog--form-dialog');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Dialog', 'FormDialog.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Dialog - LargeContent', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dialog--large-content');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dialog--large-content');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Dialog', 'LargeContent.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Dialog - Minimal', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dialog--minimal');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dialog--minimal');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Dialog', 'Minimal.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Dialog - NestedDialogs', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dialog--nested-dialogs');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dialog--nested-dialogs');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Dialog', 'NestedDialogs.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Dialog - NoOverlayClose', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dialog--no-overlay-close');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dialog--no-overlay-close');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Dialog', 'NoOverlayClose.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Dialog - WithDescription', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dialog--with-description');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dialog--with-description');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Dialog', 'WithDescription.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Dialog - WithFooter', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dialog--with-footer');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dialog--with-footer');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Dialog', 'WithFooter.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/DropdownMenu - AlignedCenter', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dropdownmenu--aligned-center');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dropdownmenu--aligned-center');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'DropdownMenu', 'AlignedCenter.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/DropdownMenu - AlignedEnd', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dropdownmenu--aligned-end');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dropdownmenu--aligned-end');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'DropdownMenu', 'AlignedEnd.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/DropdownMenu - AlignedStart', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dropdownmenu--aligned-start');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dropdownmenu--aligned-start');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'DropdownMenu', 'AlignedStart.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/DropdownMenu - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dropdownmenu--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dropdownmenu--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'DropdownMenu', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/DropdownMenu - InTableContext', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dropdownmenu--in-table-context');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dropdownmenu--in-table-context');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'DropdownMenu', 'InTableContext.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/DropdownMenu - KeyboardNavigation', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dropdownmenu--keyboard-navigation');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dropdownmenu--keyboard-navigation');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'DropdownMenu', 'KeyboardNavigation.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/DropdownMenu - ManyItems', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dropdownmenu--many-items');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dropdownmenu--many-items');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'DropdownMenu', 'ManyItems.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/DropdownMenu - OnlyDestructive', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dropdownmenu--only-destructive');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dropdownmenu--only-destructive');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'DropdownMenu', 'OnlyDestructive.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/DropdownMenu - WithDestructiveAction', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dropdownmenu--with-destructive-action');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dropdownmenu--with-destructive-action');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(
			['components', 'ui', 'radix', 'DropdownMenu', 'WithDestructiveAction.png'],
			{ maxDiffPixels: 100 }
		);
	});

	test('UI/Radix/DropdownMenu - WithDisabledItem', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dropdownmenu--with-disabled-item');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dropdownmenu--with-disabled-item');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'DropdownMenu', 'WithDisabledItem.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/DropdownMenu - WithGhostButton', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dropdownmenu--with-ghost-button');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dropdownmenu--with-ghost-button');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'DropdownMenu', 'WithGhostButton.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/DropdownMenu - WithOutlineButton', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-dropdownmenu--with-outline-button');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-dropdownmenu--with-outline-button');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'DropdownMenu', 'WithOutlineButton.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Label - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-label--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-label--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Label', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Label - FormExample', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-label--form-example');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-label--form-example');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Label', 'FormExample.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Label - Required', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-label--required');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-label--required');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Label', 'Required.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Label - WithCheckbox', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-label--with-checkbox');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-label--with-checkbox');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Label', 'WithCheckbox.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Label - WithComplexContent', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-label--with-complex-content');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-label--with-complex-content');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Label', 'WithComplexContent.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Label - WithDisabledInput', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-label--with-disabled-input');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-label--with-disabled-input');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Label', 'WithDisabledInput.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Label - WithError', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-label--with-error');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-label--with-error');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Label', 'WithError.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Label - WithHelperText', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-label--with-helper-text');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-label--with-helper-text');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Label', 'WithHelperText.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Label - WithRequiredInput', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-label--with-required-input');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-label--with-required-input');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Label', 'WithRequiredInput.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Label - WithSelect', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-label--with-select');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-label--with-select');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Label', 'WithSelect.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Label - WithTextInput', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-label--with-text-input');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-label--with-text-input');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Label', 'WithTextInput.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Popover - AlignEnd', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-popover--align-end');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-popover--align-end');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Popover', 'AlignEnd.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Popover - AlignStart', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-popover--align-start');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-popover--align-start');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Popover', 'AlignStart.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Popover - ColorPicker', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-popover--color-picker');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-popover--color-picker');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Popover', 'ColorPicker.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Popover - Controlled', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-popover--controlled');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-popover--controlled');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Popover', 'Controlled.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Popover - DateRangePicker', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-popover--date-range-picker');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-popover--date-range-picker');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Popover', 'DateRangePicker.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Popover - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-popover--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-popover--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Popover', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Popover - HelpInfo', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-popover--help-info');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-popover--help-info');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Popover', 'HelpInfo.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Popover - ModalPopover', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-popover--modal-popover');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-popover--modal-popover');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Popover', 'ModalPopover.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Popover - SettingsMenu', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-popover--settings-menu');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-popover--settings-menu');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Popover', 'SettingsMenu.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Popover - ShareMenu', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-popover--share-menu');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-popover--share-menu');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Popover', 'ShareMenu.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Popover - SideBottom', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-popover--side-bottom');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-popover--side-bottom');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Popover', 'SideBottom.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Popover - SideLeft', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-popover--side-left');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-popover--side-left');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Popover', 'SideLeft.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Popover - SideRight', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-popover--side-right');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-popover--side-right');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Popover', 'SideRight.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Popover - SideTop', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-popover--side-top');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-popover--side-top');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Popover', 'SideTop.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Popover - UserProfile', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-popover--user-profile');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-popover--user-profile');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Popover', 'UserProfile.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Popover - WithForm', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-popover--with-form');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-popover--with-form');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Popover', 'WithForm.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/RadioGroup - AllDisabled', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-radiogroup--all-disabled');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-radiogroup--all-disabled');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'RadioGroup', 'AllDisabled.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/RadioGroup - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-radiogroup--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-radiogroup--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'RadioGroup', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/RadioGroup - Horizontal', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-radiogroup--horizontal');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-radiogroup--horizontal');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'RadioGroup', 'Horizontal.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/RadioGroup - InFormContext', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-radiogroup--in-form-context');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-radiogroup--in-form-context');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'RadioGroup', 'InFormContext.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/RadioGroup - Interactive', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-radiogroup--interactive');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-radiogroup--interactive');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'RadioGroup', 'Interactive.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/RadioGroup - KeyboardNavigation', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-radiogroup--keyboard-navigation');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-radiogroup--keyboard-navigation');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'RadioGroup', 'KeyboardNavigation.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/RadioGroup - LongLabels', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-radiogroup--long-labels');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-radiogroup--long-labels');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'RadioGroup', 'LongLabels.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/RadioGroup - ManyOptionsHorizontal', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-radiogroup--many-options-horizontal');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-radiogroup--many-options-horizontal');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'RadioGroup', 'ManyOptionsHorizontal.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/RadioGroup - ManyOptionsVertical', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-radiogroup--many-options-vertical');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-radiogroup--many-options-vertical');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'RadioGroup', 'ManyOptionsVertical.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/RadioGroup - PaymentMethod', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-radiogroup--payment-method');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-radiogroup--payment-method');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'RadioGroup', 'PaymentMethod.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/RadioGroup - Required', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-radiogroup--required');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-radiogroup--required');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'RadioGroup', 'Required.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/RadioGroup - ThemeSelection', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-radiogroup--theme-selection');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-radiogroup--theme-selection');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'RadioGroup', 'ThemeSelection.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/RadioGroup - Vertical', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-radiogroup--vertical');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-radiogroup--vertical');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'RadioGroup', 'Vertical.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/RadioGroup - WithDisabledOption', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-radiogroup--with-disabled-option');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-radiogroup--with-disabled-option');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'RadioGroup', 'WithDisabledOption.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Select - ComparisonWithNative', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-select--comparison-with-native');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-select--comparison-with-native');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Select', 'ComparisonWithNative.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Select - CustomStyling', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-select--custom-styling');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-select--custom-styling');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Select', 'CustomStyling.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Select - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-select--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-select--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Select', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Select - Disabled', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-select--disabled');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-select--disabled');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Select', 'Disabled.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Select - FullWidth', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-select--full-width');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-select--full-width');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Select', 'FullWidth.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Select - InFormContext', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-select--in-form-context');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-select--in-form-context');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Select', 'InFormContext.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Select - ManyOptions', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-select--many-options');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-select--many-options');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Select', 'ManyOptions.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Select - NarrowWidth', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-select--narrow-width');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-select--narrow-width');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Select', 'NarrowWidth.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Select - Required', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-select--required');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-select--required');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Select', 'Required.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Select - WithValue', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-select--with-value');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-select--with-value');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Select', 'WithValue.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Switch - Checked', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-switch--checked');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-switch--checked');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Switch', 'Checked.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Switch - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-switch--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-switch--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Switch', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Switch - DisabledChecked', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-switch--disabled-checked');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-switch--disabled-checked');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Switch', 'DisabledChecked.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Switch - DisabledUnchecked', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-switch--disabled-unchecked');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-switch--disabled-unchecked');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Switch', 'DisabledUnchecked.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Switch - FeatureFlags', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-switch--feature-flags');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-switch--feature-flags');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Switch', 'FeatureFlags.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Switch - Interactive', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-switch--interactive');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-switch--interactive');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Switch', 'Interactive.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Switch - KeyboardNavigation', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-switch--keyboard-navigation');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-switch--keyboard-navigation');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Switch', 'KeyboardNavigation.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Switch - MultipleInList', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-switch--multiple-in-list');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-switch--multiple-in-list');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Switch', 'MultipleInList.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Switch - PrivacySettings', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-switch--privacy-settings');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-switch--privacy-settings');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Switch', 'PrivacySettings.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Switch - Required', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-switch--required');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-switch--required');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Switch', 'Required.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Switch - SettingsPanel', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-switch--settings-panel');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-switch--settings-panel');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Switch', 'SettingsPanel.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Switch - WithLabel', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-switch--with-label');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-switch--with-label');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Switch', 'WithLabel.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Switch - WithLabelAndDescription', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-switch--with-label-and-description');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-switch--with-label-and-description');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Switch', 'WithLabelAndDescription.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tabs - Dashboard', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tabs--dashboard');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tabs--dashboard');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tabs', 'Dashboard.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tabs - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tabs--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tabs--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tabs', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tabs - KeyboardNavigation', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tabs--keyboard-navigation');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tabs--keyboard-navigation');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tabs', 'KeyboardNavigation.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tabs - ManyTabs', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tabs--many-tabs');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tabs--many-tabs');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tabs', 'ManyTabs.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tabs - ProductDetails', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tabs--product-details');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tabs--product-details');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tabs', 'ProductDetails.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tabs - SettingsPage', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tabs--settings-page');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tabs--settings-page');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tabs', 'SettingsPage.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tabs - Vertical', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tabs--vertical');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tabs--vertical');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tabs', 'Vertical.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tabs - VerticalSidebar', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tabs--vertical-sidebar');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tabs--vertical-sidebar');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tabs', 'VerticalSidebar.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tabs - WithDefaultValue', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tabs--with-default-value');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tabs--with-default-value');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tabs', 'WithDefaultValue.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tabs - WithDisabledTab', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tabs--with-disabled-tab');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tabs--with-disabled-tab');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tabs', 'WithDisabledTab.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - Default', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--default');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--default');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'Default.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - HelpIcon', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--help-icon');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--help-icon');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'HelpIcon.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - InFormContext', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--in-form-context');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--in-form-context');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'InFormContext.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - InGridLayout', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--in-grid-layout');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--in-grid-layout');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'InGridLayout.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - LongDelay', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--long-delay');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--long-delay');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'LongDelay.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - LongText', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--long-text');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--long-text');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'LongText.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - MultipleTooltips', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--multiple-tooltips');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--multiple-tooltips');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'MultipleTooltips.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - OnDestructiveButton', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--on-destructive-button');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--on-destructive-button');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'OnDestructiveButton.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - OnDisabledButton', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--on-disabled-button');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--on-disabled-button');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'OnDisabledButton.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - OnGhostButton', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--on-ghost-button');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--on-ghost-button');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'OnGhostButton.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - OnIconButton', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--on-icon-button');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--on-icon-button');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'OnIconButton.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - OnOutlineButton', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--on-outline-button');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--on-outline-button');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'OnOutlineButton.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - OnText', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--on-text');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--on-text');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'OnText.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - ShortDelay', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--short-delay');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--short-delay');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'ShortDelay.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - SideBottom', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--side-bottom');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--side-bottom');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'SideBottom.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - SideLeft', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--side-left');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--side-left');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'SideLeft.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - SideRight', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--side-right');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--side-right');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'SideRight.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/Radix/Tooltip - SideTop', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-radix-tooltip--side-top');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-radix-tooltip--side-top');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'radix', 'Tooltip', 'SideTop.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ThemeToggle - BothStates', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-themetoggle--both-states');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-themetoggle--both-states');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ThemeToggle', 'BothStates.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ThemeToggle - DarkMode', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-themetoggle--dark-mode');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-themetoggle--dark-mode');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ThemeToggle', 'DarkMode.png'], {
			maxDiffPixels: 100,
		});
	});

	test('UI/ThemeToggle - LightMode', async ({ page }) => {
		// STRICT MODE: Verify story exists BEFORE navigation (fast fail if missing)
		await ensureStoryExists(page, 'ui-themetoggle--light-mode');

		// Navigate to story (relative URL - Playwright resolves with baseURL)
		await page.goto('/iframe.html?id=ui-themetoggle--light-mode');

		// Wait for story to fully render
		await page.waitForLoadState('networkidle');

		// Take screenshot and compare
		await expect(page).toHaveScreenshot(['components', 'ui', 'ThemeToggle', 'LightMode.png'], {
			maxDiffPixels: 100,
		});
	});
});

/**
 * Basename of the bundle this CLI is shipped as. Used to tell our own
 * LAUNCHER_BUNDLE_OVERRIDE from one leaked by another CLI's launcher.
 * See shared-common/utils/resolveOwnBundlePath.
 */
export const TASK_BUNDLE_NAME = 'task.cjs';
